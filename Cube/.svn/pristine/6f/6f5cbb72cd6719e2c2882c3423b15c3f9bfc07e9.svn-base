#pragma once
#include "Exit.h"

enum zaleznosc
{
	suma_wieksza,
	suma_mniejsza,
	podzielnosc,
	nie_ma_cyfry,
	permutacja
};
// class representing the object that shows numeric hint on use
class Column :
	public Physical
{
private:
	zaleznosc wskazowka;
	bool prawdziwosc;
	char a;
	char x;
	char y;
	char z;
	sf::Text hint_display;
	int hint_lifetime;
public:
	Column();
	Column(int ID, Sprite_Sheet_Data base, sf::Vector2f position, World<Physical> *mapa);
	virtual void activate(Physical *target);
	void useit(Physical *user);
	void update(World<Physical>*mapa);
	void hint_display_update(World<Physical>* mapa);
	~Column();
};

